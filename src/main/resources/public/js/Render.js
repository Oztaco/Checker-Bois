/**
 *  Handles the rendering of the checkers board and animations
 */


renderBoard = function(canvasElm, checkerBoard) {
    ctx = canvasElm.getContext('2d');
    height = canvsElm.height;
    width = canvsElm.width;

    for (var x = 0; x < 8; x++) {
        for (var y = 0; y < 8; y++) {
            
        }
    }
}


themes = [
    {
        primaryColor: "#f00",
        secondaryColor: "#fff"
    }
]
